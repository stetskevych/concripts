server {
  listen   80;
  server_name  <%= node['hostname'] %>;

  access_log  <%= node['nginx']['log_dir'] %>/localhost.access.log;

  location / {
    root   /var/www/<%= node['nginx-static']['deploy_dir'] %>;
    index  index.html;
  }
}
